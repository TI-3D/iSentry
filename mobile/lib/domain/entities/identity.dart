class Identity {
  final int id;
  final String name;
  final bool key;
  final DateTime createdAt;
  final DateTime updatedAt;

  Identity({
    required this.id,
    required this.name,
    required this.key,
    required this.createdAt,
    required this.updatedAt,
  });
}
